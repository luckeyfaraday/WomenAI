name: Android Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install Dependencies
        working-directory: ./projects/WomenAI/frontend
        run: npm ci

      - name: Build Web App
        working-directory: ./projects/WomenAI/frontend
        run: npm run build

      - name: Sync Capacitor
        working-directory: ./projects/WomenAI/frontend
        run: npx cap sync android

      - name: Build Android APK
        working-directory: ./projects/WomenAI/frontend/android
        run: ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: app-debug
          path: projects/WomenAI/frontend/android/app/build/outputs/apk/debug/app-debug.apk
